<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="@color/turqoise"
    tools:context=".SearchManualActivity" >

   
    <TextView
        android:id="@+id/search_manual_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/search_manual_title"
        android:layout_centerHorizontal="true"
    />
    
      <ListView
          android:id="@+id/search_manual_listview"
          android:layout_width="fill_parent"
          android:layout_height="wrap_content"
          android:layout_below="@id/search_manual_title"
          android:layout_marginTop="20dp"
          android:background="@color/silver"
          android:cacheColorHint="@android:color/transparent"
          android:divider="@color/turqoise"
          android:dividerHeight="10dp"
          android:paddingLeft="2dp" />

</RelativeLayout>
